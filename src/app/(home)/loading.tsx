'use client';

import { HomepageSkeleton } from '@/components/skeleton';

/**
 * Homepage Loading State
 * Displays editorial skeleton while homepage data fetches
 */
export default function HomeLoading() {
    return <HomepageSkeleton />;
}
