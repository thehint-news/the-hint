'use client';

import { SectionPageSkeleton } from '@/components/skeleton';

/**
 * Section Page Loading State
 * Displays editorial skeleton while section data fetches
 */
export default function SectionLoading() {
    return <SectionPageSkeleton />;
}
